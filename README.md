# Incident AI Android build bootstrap

This first scaffold increment adds the checksum-pinned Gradle 8.13 wrapper.
Use JDK 17 and run `./gradlew --version` to verify the build launcher.
The application modules follow in a separately reviewed scaffold increment.
